# JWT Auth Stack — Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit the actual .env file.

JWT_SECRET="change-me-use-a-random-256-bit-value-in-production"
JWT_ISSUER="nexusflow-jwt-lab"
JWT_AUDIENCE="nexusflow-lab-clients"
JWT_TTL="15m"
POSTGRES_PASSWORD="change-me-strong-password"
UID=1000
GID=1000
